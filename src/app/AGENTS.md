This folder contains the app routing structure.

General rules:

- `(tabs)` is for screens that use tab navigation.
- `(screens)` is for regular screens that do not use tabs.

Route names should stay consistent with the files and layout definitions in each group.
