This folder is the home for shared constants.

It also contains `theme.ts`, which defines the app's design system tokens such as colors, fonts, spacing, and layout values.

Keep values here centralized so the rest of the app can depend on one source of truth for visual consistency.
